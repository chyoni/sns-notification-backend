version = "1.0.0"
description = "Notification Consumer"

dependencies {
    implementation(project(":notification-core"))
    implementation(project(":notification-commons"))

    implementation(libs.spring.stream.kafka)
    implementation(libs.springdoc)
}